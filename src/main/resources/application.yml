quarkus:
  cache:
    caffeine:
      "greeting":
        expire-after-write: 10s
        initial-capacity: 1
        maximum-size: 1
        metrics-enabled: true
  scheduler:
    metrics:
      enabled: true

cache:
  greeting:
    refresh-cron: 0/20 * * * * ? *